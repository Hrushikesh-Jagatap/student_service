const Employee = require('./Employee');
const Users = require('./Student');
const Address = require('./Address');
const Favourite = require('./Favourite');
const Batch = require('./Batch');
const AppVersion = require('./AppVersion');

module.exports = {
  Employee,
  Users,
  Address,
  Favourite,
  Batch,
  AppVersion,
};
